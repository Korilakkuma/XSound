'use strict';

describe('XSound TEST', () => {
    describe('XSound.toString', () => {
        it('should return "[XSound]"', () => {
            expect(X.toString()).toEqual('[XSound]');
        });
    });
});
